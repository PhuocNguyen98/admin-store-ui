import routes from './routes';
import { category, supplier, discount, product, staff } from './routes';

const config = { routes, category, supplier, discount, product, staff };

export default config;
